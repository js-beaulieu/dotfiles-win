#!/usr/bin/env bash

# install packages
sudo pacman -Syu --noconfirm
sudo pacman -S --noconfirm --needed - < "$HOME/.config/yadm/bootstrap.d/pacman.txt"

# change default shell to fish
#echo "INFO" "Change default shell to fish"
sudo chsh -s /usr/bin/fish "$(whoami)"

