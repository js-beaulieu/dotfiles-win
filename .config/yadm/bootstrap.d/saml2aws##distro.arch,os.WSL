#!/usr/bin/env bash
. "$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd)/.global.sh"

# dependencies
log "Install dependencies"
sudo pacman -Syu --noconfirm
sudo pacman -S --noconfirm --needed pass gnupg fish

# generate gnupg key


