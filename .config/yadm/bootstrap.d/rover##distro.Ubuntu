#!/usr/bin/env bash
. "$(cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd)/.global.sh"


# dependencies
log "Install dependencies"
sudo apt update
sudo apt install -y curl

# install rover
log "Installing rover"
curl -sSL https://rover.apollo.dev/nix/latest | sh -s -- --force

